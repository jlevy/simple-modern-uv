# {{ package_name }}

{{ package_description }}

## Setup

Requires [uv](https://docs.astral.sh/uv/).

```shell
# 1. Clone the repository and navigate into it
# 2. Install dependencies
make install
```

This command creates a virtual environment in `.venv` and installs all necessary packages defined in `pyproject.toml`.

## Development Workflow

This template is optimized for VS Code and includes a recommended configuration in `.vscode/settings.json` that enables format-on-save.

- **Lint & Format:** `make lint`
- **Run Tests:** `make test`

*This project was built from a streamlined version of [simple-modern-uv](https://github.com/jlevy/simple-modern-uv).*
